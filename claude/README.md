claude の設定を忘れがちなので、メモとして残しておきます。

# 前提

- `claude -w` は、`HEAD` になっているブランチを基準に worktree を使用します。
- そのため、作業を始める前に `gitsetheaad` でベースとなるブランチを `HEAD` に切り替えておきます。
- `.gitignore` は、ルートまたはプロジェクト側のどちらかに記述します。

# その他

`.worktreeinclude` は、各ワークツリーで共通に読み込ませたい設定をまとめるためのファイル。必要に応じて調整。
